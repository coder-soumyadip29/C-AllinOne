#include<stdio.h>
int main(){
float b,h;
printf("Enter the base of triangle: ");
scanf("%f",&b);
printf("Enter the height of triangle: ");
scanf("%f",&h);
printf("The are of triangle is: %f",b*h*1/2);

return 0;
}